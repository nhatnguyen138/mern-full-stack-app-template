# mern-full-stack-app-template
MERN full stack app template (TypeScript template)

To run client, run
``` Linux
$ cd client
$ npm install
$ npm start
```

To run server, run
``` Linux
$ cd server
$ npm install
$ npx yarn watch
$ npx yarn dev
```
